# BASISForum 移动触控与滚动验证

**验证日期：2026-08-14 · 执行者：Manus AI**

## 结论

BASISForum 已在部署后的移动设备仿真中完成触摸滑动、横向溢出、导航触控目标和紧凑下拉控件命中区域检查。测试确认公共首页在 iPhone 13 风格与 Pixel 5 风格视口中**没有横向溢出**，合成触摸滑动可以推动页面滚动，并且移动顶部导航和语言／显示模式选择器的实际交互容器均已扩展到 **44px** 高度。

本次检查发现并修复了顶部导航按钮原先为 38px 高的触控目标。修复后，Search 和导航 Sign in 按钮在两种仿真设备中都测得为 44px 高。紧凑选择器可见文字层本身仅约 16px 高，但其直接的 MUI 输入根节点以及外层命中容器均为 44px 高，因此可点按区域并非 16px。

> 该报告基于 Chromium 中的 Playwright 设备仿真。它模拟 iPhone/Pixel 的视口、设备像素比、移动 UA、coarse pointer 与触摸输入，但**不能替代真实 iOS WebKit/Safari 或真实 Android 硬件测试**。

## 测试矩阵

| 设备仿真 | CSS 视口 | 触摸滚动位移 | 横向溢出 | 帧采样最大间隔 | 结论 |
|---|---:|---:|---|---:|---|
| iPhone 13 风格 | 390 × 664 | 190px | 无 | 33.3ms | 滑动有效；首轮帧采样出现单次约 33ms 间隔，未在本次短样本中观察到持续性卡顿。 |
| Pixel 5 风格 | 393 × 727 | 127px | 无 | 16.8ms | 滑动有效；帧采样保持在约一帧间隔。 |

页面的总可滚动高度分别为 848px；触摸手势之后的 `window.scrollY` 均增加，说明滚动手势没有被导航条、讨论行或页面容器错误拦截。页面 `scrollWidth` 与 `clientWidth` 完全一致，因此移动视口没有隐藏的横向滚动。

## 触控目标与交互检查

| 控件 | 修复前 | 修复后 | 验证结果 |
|---|---:|---:|---|
| 顶部 Search 按钮 | 38px 高 | 44px 高 | 通过 |
| 顶部 Sign in 按钮 | 38px 高 | 44px 高 | 通过 |
| 语言选择器有效命中容器 | 未明确保证 | 44px 高 | 通过 |
| 显示模式选择器有效命中容器 | 未明确保证 | 44px 高 | 通过 |
| 首页 Sign in、Latest、Hot 控件 | 约 43px 高 | 未变更 | 通过 WCAG 2.5.8 的 24px 最低目标；保留既有 Swiss 紧凑排版。 |

本次修复将移动导航按钮的最小高度设为 44px，并将两个 MUI `Select` 的输入根与外层容器统一到 44px。由于 MUI 使用可见文字节点配合更大的输入根来接收命中事件，只测量带 `role="combobox"` 的文字节点会低估真实可触控面积；层级诊断已确认其父元素和外层容器均为 44px。

## 性能与兼容性说明

短时 `requestAnimationFrame` 采样用于发现明显的连续主线程阻塞，而非替代真实设备上的长时性能分析。Pixel 5 风格样本的最大帧间隔为 16.8ms。iPhone 13 风格样本包含一次约 33.3ms 间隔；考虑到测试在页面初始化后立即采样，当前证据不足以将其判定为滚动卡顿。建议在真实 iPhone Safari 上连续滚动长讨论页 30–60 秒，再用 Safari Web Inspector 复核长任务、内存和合成层。

Android 风格的显示模式选择器在页面水合后能够通过触摸事件打开。iPhone 风格仿真中的同一选择器在无头 Chromium 环境下存在不稳定的原生菜单可观测性，因此不将该项作为 Safari 功能失败；实际的 44px 命中容器、无横向溢出和有效滑动已得到确认。

## 已发布变更

- `packages/ui/src/components/index.tsx`：移动导航操作的最小高度统一为 44px。
- `packages/ui/src/LanguageProvider.tsx`：语言选择器的输入根和命中容器统一为 44px。
- `packages/ui/src/DisplayModeProvider.tsx`：显示模式选择器的输入根和命中容器统一为 44px。

该修复已通过 23 个单元测试、零警告 lint、Web/Admin 严格顺序生产构建，并已部署到生产服务。
